# Copy this file to android/keystore.properties and fill in your real values.
# This file (the .example) is committed; keystore.properties itself is gitignored.
# Generate the keystore first with the keytool command in PLAY-RELEASE-GUIDE.txt.

storeFile=/Users/whitzy/veilbreak/veilbreak-upload.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=veilbreak-upload
keyPassword=YOUR_KEY_PASSWORD
